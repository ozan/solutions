
# compile and execute haskell
function hs {
  ghc -o tmp "$1" && ./tmp
}


[[ $BASH_SOURCE = "$0" ]] && {
  # show help if no sub-command provided
  [[ $# -eq 0 ]] && { help; exit; }
  # otherwise invoke the fun
  eval "$1" "${@:2}"
}
